"""
Shared pytest config. Intentionally empty — the current suite is pure-function
only and needs no fixtures. Add DB/app fixtures here when integration tests
are reintroduced (post-MVP, see docs/testing/README.md).
"""
