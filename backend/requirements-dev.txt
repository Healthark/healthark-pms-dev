# Dev / CI dependencies. Not needed for production deployments.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
httpx==0.27.2
pytest-cov==5.0.0
ruff==0.7.4
